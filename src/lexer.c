#include "lexer.h"


int lex_table_add(const char* str, int len) {
   for (int i = 0; i < len && str[i] != '\0'; i++) {
      lexeme_t
   }
}




int* _get_next_quotes(const char* str, size_t len, int i);
int _increment_counter(const char* str, size_t len, int i, bool init);
